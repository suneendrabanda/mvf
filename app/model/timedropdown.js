
Ext.define("MVF.model.timedropdown", {
    extend: "Ext.data.Model",
    config: {
        fields: [
            { name: "text", type: "string" },
            { name: "value", type: "string" }
           ]
    }
});
